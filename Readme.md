# gererio application summary

## Usage

Just use it.  
Asumes you have node and npm installed

to install:
```
sudo npm install
````
then to run:

`npm start`

to test : 

`npm test`
